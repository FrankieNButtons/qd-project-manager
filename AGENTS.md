# Repo Instructions

## Session Cleaner Output

When using the `session-cleaner` skill, always present every scanned session in a compact TUI-style table.
Do not only show candidate counts or a summary when scan results exist.
Include the full scanned list before any delete action.
